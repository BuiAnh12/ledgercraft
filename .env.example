# OLTP Postgres
OLTP_DB=ledgercraft_oltp
OLTP_USER=app
OLTP_PASSWORD=app_password
OLTP_PORT=5432

# DWH Postgres
DWH_DB=ledgercraft_dwh
DWH_USER=warehouse
DWH_PASSWORD=warehouse_password
DWH_PORT=5433

# Redis
REDIS_PORT=6379

# Kafka / Connect / Schema Registry
KAFKA_BROKER_HOST_PORT=29092    # Host port -> broker's external listener
SCHEMA_REGISTRY_HOST_PORT=8085  # Host port for Schema Registry (container listens on 8081)
KAFKA_CONNECT_HOST_PORT=8083    # Connect REST
KAFKA_UI_HOST_PORT=8082         # Optional Kafka UI
